name: Update Debian
on:
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  call-reusable:
    uses: ./.github/workflows/reusable-cloudflared-ansible.yml
    with:
      ansible_dir: "debian"
      ansible_command: "update_debian.yml"
    secrets: inherit